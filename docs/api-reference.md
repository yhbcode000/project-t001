# API Reference

The documentation site includes `mkdocstrings` so Python API docs can be generated from source docstrings when the services grow beyond the current Hello World scaffold.

## FastAPI application

::: app.main
    options:
      show_source: false

## Future API pages

Additional generated API pages can be added as service packages mature. The current scaffold keeps service documentation focused on architecture and runtime behavior while the Python APIs remain intentionally small.
